# Implementation Plan: Release v2.4.3 (Issue #107)

- **Goal**: Bump version to `2.4.3`, synchronize descriptors, update changelog, run full regression tests, merge PR, and push release tag to produce native Android APKs and dual GHCR tags.
- **Architecture**: SemVer bump across Node.js, Android Gradle, PWA Service Worker, and CI/CD pipelines.

## Definition of Ready (DoR) Checklist
- [x] Spec & Issue #107 approved and tracked.
- [x] Baseline test suite passing (257 tests, 0 failures).
- [x] Topic branch `chore/release-v2.4.3` created.
- [x] Zero placeholders or ambiguous requirements.

## Task Breakdown

### Task 1: Version Synchronization
- Update `package.json` -> `2.4.3`
- Update `manifest.json` -> `2.4.3`
- Update `android/app/build.gradle` -> `versionCode 243`, `versionName "2.4.3"`
- Update `public/sw.js` -> `CACHE_NAME = 'tuneflow-v2.4.3'`
- Update `README.md` and `README.vi.md` -> `2.4.3`
- Update `.github/workflows/release.yml` -> `default: 'v2.4.3'` and `Run Full Test Suite (257 Tests)`
- Update test files expecting `2.4.2` -> `2.4.3` (`tests/ci-cd-unified-pipeline.test.js`, `tests/objective-deployment-and-cicd-alignment.test.js`)

### Task 2: Changelog Update
- Append `v2.4.3` release section to `CHANGELOG.md` highlighting Issues #97, #99, #101, #103, #105, and #107.

### Task 3: Automated Verification
- Run full regression suite (`node --test tests/*.test.js`) -> assert 257+ passing tests.
- ESLint audit -> assert 0 errors, 0 warnings.

### Task 4: Merge PR & Tag Release
- Create PR, squash-merge into `master`.
- Tag `v2.4.3` with release message.
- Push tag to origin and monitor GitHub Actions execution.
